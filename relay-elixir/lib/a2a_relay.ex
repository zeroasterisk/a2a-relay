defmodule A2aRelay do
  @moduledoc """
  A2A Relay Server — Elixir/OTP implementation.

  Bridges A2A agents (connected via WebSocket) with clients (via HTTP JSON-RPC).
  Provides offline queuing, JWT authentication, and multi-tenant isolation.
  """
end
